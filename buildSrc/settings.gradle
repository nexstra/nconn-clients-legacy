pluginManagement {
  repositories{
    //maven { url "${rootProject.projectDir}/repo" }
    maven { url "${rootProject.projectDir}/repo" }
    gradlePluginPortal()
  }
}

//includeBuild "../../../docstore"
